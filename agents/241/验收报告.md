# EAS-31 验收报告（实施侧）——241-初始数据生成与修改agent

本报告只记录脱敏 Fixture 的可复算结论；不包含运行期原始数据、SQLite、模型响应或密钥。

## 结论

241 已实现为确定性生成 + 硬代码校验 + LLM 候选值注入的绑定数据生成器：事件模式消费 220 结构闭包、230 操作闭包与只读快照；Foundation 模式消费 210 画像、220 结构闭包、CA-V0.3.0、TRG-V1.0.0 与快照且拒绝操作闭包。输出唯一 `bound_data` 交 242，242/260 反馈只生成新版本、旧包不覆盖，第 3 次阻断。代码、包 Schema、Prompt、测试、脱敏 Fixture、manifest 模板与 Multica Agent 定义齐全。

## 冻结输入

- 约束资产 CA-V0.3.0、带类型引用图 TRG-V1.0.0。
- 机器权威：`config/v5-architecture.json`、`config/v5-package-catalog.json`、`governance-manifest.json`（EAS-44 PR #7，merge `0d66e01`）。
- 本 Issue 在 `v5-package-catalog.json` 追加 3 个消费合同包类型（EAS-50 同类扩展）：`database_read_snapshot`（EAS-19→241）、`data_validation_failed_feedback`（242→241）、`sql_regression_failed_feedback`（260→241）。`bound_data`/`structure_closure`/`operation_closure`/`foundation_profile` 已在目录中，未改动。

## 包合同（新增 `contracts/packages/`）

| Schema | 用途 |
|---|---|
| `bound-data-package.schema.json` | 241 输出，字段逐条对应 DATA-PENDING-VALIDATION |
| `data-validation-failed-feedback.schema.json` | 242→241 验证失败反馈 |
| `sql-regression-failed-feedback.schema.json` | 260→241 回归失败反馈 |
| `database-read-snapshot.schema.json` | EAS-19 只读查询结果快照（241 复用现有记录） |
| `bound-data-manifest.schema.json` + `bound-data-manifest-template.json` | 运行期 locator 控制面 manifest |

COMMON-ENVELOPE 复用 `contracts/common/common-envelope.schema.json`（未改动）。`structure_closure`/`operation_closure` 的 payload 直接按 EAS-44 冻结的 `contracts/v5-runtime-packages.schema.json` `$defs` 校验（未改动该文件）。FOUNDATION-TASK-PACKAGE 由 EAS-44 以 `foundation_profile` 取代，未另建重复 Schema。

## 实现

- `src/east_v5/agents/241/generator.py`：`BoundDataGenerator`（输入门、确定性数据组、硬代码校验、242/260 回退重铸、manifest）。
- `src/east_v5/agents/241/probe.py`：脱敏可复算探针（`python3 -m east_v5.agents.241.probe`）。

## 测试证据

`python3 scripts/v5.py check`：通过（213 tests，含 241 的 27 条）。`git diff --check`：通过。241 专项覆盖：

- 事件/Foundation 成功路径、可复算哈希（同输入同 `content_hash`）。
- 模式门：Foundation 拒绝操作闭包（`FOUNDATION_OPERATION_CLOSURE_FORBIDDEN`）、事件缺操作闭包（`OPERATION_CLOSURE_REQUIRED`）。
- 输入拒绝：坏哈希（`CONTENT_HASH_DRIFT`）、未知字段、孤儿 existing/temporary/record_link、表/字段越闭包、`CASE_ROLE_INVALID`、空值/类型不一致、血缘为空、汇总不一致。
- 回退：242 反馈 → `version+1`+`supersedes_ref`+`attempt=2`；260 回归（route=241）→ `attempt=3`+`blocked_manual`；错绑数据包引用、route 非 241、attempt 越界、缺失 proposed 均拒绝。
- manifest：合法、issue_key 不匹配、越界 locator 拒绝。
- 下游 242 Stub 真实消费 `bound_data`。

## 脱敏最小真实运行（probe）

`PYTHONPATH=src python3 -m east_v5.agents.241.probe` 摘要：

```json
{
  "artifact_ref": {"artifact_id": "bound-data-eas31-sanitized-run", "version": 3, "content_hash": "ac2fdb64720acac3a40f697f219acb839437840c659d1ecf7156d21e49016f3d"},
  "event_records": 2, "foundation_records": 1,
  "attempt2_remapped": true, "attempt3_blocked": true,
  "bad_hash_rejected": true, "foundation_operation_rejected": true,
  "manifest_ok": true, "stub_242_consumed": "2-records"
}
```

## Multica Agent 定义

| 属性 | 值 |
|---|---|
| 名称 | `241-初始数据生成与修改agent` |
| UUID | `7df640f9-973f-4c46-8302-df1256f60146` |
| 运行时 | DeepSeek `0e5e9dd9-5135-4937-bb03-92b77adb8395`，model 继承运行时默认 |
| 最大并发 | 1 |
| 权限 | workspace（`public_to`） |
| Skill | `east-v5-runtime`（仓库内 SKILL，随 checkout 加载） |

## 安全与目录边界

- 仅在 `multica repo checkout` 专用 checkout 实施；未复制参考源旧实现/数据库/KB/候选产物。
- 未读取或提交 CoreBank 原始数据、真实 SQLite、模型原始响应、日志、密钥或 `.env`。
- 运行期制品只留 `${V5_RUNTIME_ROOT}/vnext/03_构建过程层`；本 Agent 零发布、零正式库写入。

## 未解析项（交下游/人工）

- `database_snapshot_ref`（EAS-19→260）与新增 `database_read_snapshot`（EAS-19→241）为两个不同包；241 只消费后者，未改动前者 consumers。
- 跨 Agent 真实任务（241 消费真实 220/230 输出）待 220/230 落地并完成下游接线后由 Sol 放行触发；当前以已批准最小 Fixture 完成合同先行测试，不冒充真实上游。
- Git 写操作（commit/push/PR）按规则交工程助理执行，本报告不包含。
