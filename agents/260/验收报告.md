## EAS-35 / EAS-60 语义集成本地验收

基线为 `b9fddab`（已含 EAS-60）。Foundation 保留完整任务包三元组、确定性 INSERT、物理 copy 隔离、事务内 delta 和回滚验证；没有回退为 profile 推断。

事件模式新增完整 110 `QUESTION-SQL-DUAL-REVIEW-PASSED` 合同、110/140 父引用和哈希校验，以及以批准脱敏 Fixture 的记录身份/角色为依据的正反例、条件/码值覆盖、行数容差、分组/DISTINCT、JOIN 倍率计算。正例和反例均显式受 `minimum_*_count` 约束，反例记录不得泄漏到结果。110 未知字段和版本漂移会在执行前拒绝；执行报告操作按 read/check/insert/update 的封闭 Schema 验证，未知嵌套字段由 210 Stub 同样拒绝。第三次 SQL 执行错误路由人工阻断；独立 210 Stub 消费事件成功包及失败反馈。

本地命令：

`PYTHONPATH=src python3 -m unittest tests.agents.260.test_regression`：15/15 通过。

`python3 scripts/v5.py check`：304/304 通过。

所有测试使用临时、脱敏 SQLite copy；没有读取参考源、CoreBank、密钥或 `.env`，也没有写正式库或提交 SQLite/日志。
