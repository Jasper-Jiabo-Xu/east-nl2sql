# EAS-27：180-GLM审核员返工验收报告

## 结论

跨路由优先级返工基于 PR #31 当前 head `6f1333807f9846d4f070e209afd2d9296e2fc5e1` 完成本地验证，等待工程助理将未提交工件更新至同一 PR。当前与新 head 均未获得 merge 授权。

## 变更与合同

- `GLMReviewerAgent.review()` 不再接收 `decision`、错误类型、详情、证据或路由；没有模型客户端即拒绝 `MODEL_CLIENT_REQUIRED`，不存在默认放行。
- 仅接受 GLM 返回的完整 JSON 文本；硬校验未知字段、固定六类枚举、`reviewer_id=180`、失败详情/证据、完整错误集到唯一下一跳的优先级映射（`120 → 130 → 140 → 150`）、输入包哈希、输出父引用和输入哈希。跨路由错误原样保留，不静默删除。
- 非法 JSON、枚举、字段或传输失败最多调用 GLM 三次；上游 attempt 1/2 耗尽显式失败，attempt 3 生成 `blocked_manual`，110 Stub 仅返回人工处置而不误路由。
- 180 独立于 170；输出身份由输入不可变三元组派生，版本与父引用不允许调用方注入。
- EAS-62 的真实 GLM 输出发现 `OBSERVABLE_MAPPING_ERROR`、`QUERY_SPEC_ERROR`、`QUESTION_SQL_ERROR`、`BUSINESS_EVENT_ERROR`；完整保留后按优先级得到唯一下一跳 `130`。原始响应只留运行数据面，PR 与本报告不复制该内容。

## Multica 运行期配置

- Agent UUID：`9639f8aa-44fa-46ac-9373-ff4085d609fc`
- 名称：`180-GLM审核员agent`
- runtime/model：Hermes `1736816d-96e2-4baf-8792-ee883ecaffed` / `zai:glm-5.1`
- 并发 1；Skill/MCP/自定义环境均为空；无数据库或正式发布权限。
- 已启动受控脱敏探针 EAS-61，task `8c41a8c7-f94e-4efe-a3ed-d9b9ae4649fe`；回调条件和唯一 callback owner 均已写入子任务，运行结果不得包含模型原始响应。

## 验证

- `PYTHONPATH=src python3 -m unittest tests/agents/180/test_reviewer.py -v`：12 passed；覆盖单路由、多路由优先级、顺序不变性、低优先级拒绝、三轮阻断与 110 Stub。
- `PYTHONPATH=src python3 -m east_v5.agents.east_180.probe`：通过 pass、六类失败路由、多错误、哈希漂移和 110 Stub 摘要。
- `python3 -m pytest --import-mode=importlib -q --ignore tests/agents/242/test_constraint_asset_receipt_contract.py`：325 passed；唯一忽略项依赖检出中不存在的顶层 `constraint_assets` 包，与本次 180 改动无关。
- `git diff --check`：通过；未发现敏感内容、数据库、原始模型响应或日志。

## 文件 SHA-256

| 文件 | SHA-256 |
|---|---|
| `agents/180/definition.md` | `ff754b9efbbabd7b12ebac65ad6fab89b731db795a4e0a8e30fa71f63bf7a721` |
| `agents/180/prompt.md` | `903cad042204b73817df837bd0de009605374d013e7c79e2fdb0ac5a697646d1` |
| `src/east_v5/agents/east_180/__init__.py` | `03b61e707bc846892b54d1a51dc2842ce0fd62c38b1b7acfe08b0a20ec32633c` |
| `src/east_v5/agents/east_180/probe.py` | `5c33ccfcb4aa8f0aab8ad2d1759eaadd6db8ee7d60e2c91bf9a32d9217e24720` |
| `src/east_v5/agents/east_180/reviewer.py` | `1d76c9e4763912243a566413b84129db222e6c187ea0767f8c764b7f4ad04c32` |
| `tests/agents/180/test_reviewer.py` | `00eb4bd29b3db081615b1fbc3d741bc64267569be3730f368ac89c539bb9cfef` |

运行前如对工件作任何修改，必须重新计算此表并重跑专项与全量回归。
