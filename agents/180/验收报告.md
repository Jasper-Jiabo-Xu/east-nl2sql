# EAS-27：180-GLM审核员返工验收报告

## 结论

返工实现已在 PR #31 的精确 head `0adeaccff30af25789cf2bed8e6df57617c33dd6` 上完成本地验证，等待工程助理将未提交工件更新至同一 PR。旧 head 不含本报告的改动，且未获得 merge 授权。

## 变更与合同

- `GLMReviewerAgent.review()` 不再接收 `decision`、错误类型、详情、证据或路由；没有模型客户端即拒绝 `MODEL_CLIENT_REQUIRED`，不存在默认放行。
- 仅接受 GLM 返回的完整 JSON 文本；硬校验未知字段、固定六类枚举、`reviewer_id=180`、失败详情/证据、错误到修复 Agent 的确定映射、输入包哈希、输出父引用和输入哈希。
- 非法 JSON、枚举、字段或传输失败最多调用 GLM 三次；上游 attempt 1/2 耗尽显式失败，attempt 3 生成 `blocked_manual`，110 Stub 仅返回人工处置而不误路由。
- 180 独立于 170；输出身份由输入不可变三元组派生，版本与父引用不允许调用方注入。

## Multica 运行期配置

- Agent UUID：`9639f8aa-44fa-46ac-9373-ff4085d609fc`
- 名称：`180-GLM审核员agent`
- runtime/model：Hermes `1736816d-96e2-4baf-8792-ee883ecaffed` / `zai:glm-5.1`
- 并发 1；Skill/MCP/自定义环境均为空；无数据库或正式发布权限。
- 已启动受控脱敏探针 EAS-61，task `8c41a8c7-f94e-4efe-a3ed-d9b9ae4649fe`；回调条件和唯一 callback owner 均已写入子任务，运行结果不得包含模型原始响应。

## 验证

- `PYTHONPATH=src python3 -m unittest tests/agents/180/test_reviewer.py -v`：9 passed。
- `PYTHONPATH=src python3 -m east_v5.agents.east_180.probe`：通过 pass、六类失败路由、多错误、哈希漂移和 110 Stub 摘要。
- `python3 -m pytest --import-mode=importlib -q --ignore tests/agents/242/test_constraint_asset_receipt_contract.py`：322 passed；唯一忽略项依赖检出中不存在的顶层 `constraint_assets` 包，与本次 180 改动无关。
- `git diff --check`：通过；未发现敏感内容、数据库、原始模型响应或日志。

## 文件 SHA-256

| 文件 | SHA-256 |
|---|---|
| `agents/180/definition.md` | `fd83cfd8b3c7b1e96d99303ab7f7c72fe85df2dc80b757d9bead8d3f54ede684` |
| `agents/180/prompt.md` | `69f44c2a9759fc61d184aca28d91d70f68155f8c477a037baa3a36d8b377170e` |
| `src/east_v5/agents/east_180/__init__.py` | `09d6521d0fa401ee943cf27b6be9bafbb9e0ecd091d7e75fecdd1ab3981c9095` |
| `src/east_v5/agents/east_180/probe.py` | `1e8aa3ac703df6ac172fe7d32ea7687e297b4231b6a3addf6f9f4c5011757aea` |
| `src/east_v5/agents/east_180/reviewer.py` | `d12f285bfe701111d3e74afcfe9164b7c5707026c5f69be8d599f0821660dd8f` |
| `tests/agents/180/test_reviewer.py` | `107c83ff9ce3a6a323f2b60dec3ed6bb8e11f39549a74a752e034908adb3d8c1` |

运行前如对工件作任何修改，必须重新计算此表并重跑专项与全量回归。
