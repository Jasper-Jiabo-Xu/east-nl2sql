# Agent 010：固定发布提交

- 输入：210 组装且已通过 260 的 `release_candidate`。
- 输出：`release_receipt`、正式版本、manifest 与哈希。
- 验收：来源、审批、SQLite/JSON 校验、回归报告、候选内容哈希和目标版本全部一致。
- 拒绝：任何未审批、哈希漂移、敏感数据、未知字段或非快进发布。
- 权限：唯一允许提交正式库的固定代码；不得让模型自由生成发布 SQL。
