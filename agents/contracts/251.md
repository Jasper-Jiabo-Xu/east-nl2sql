# Agent 251：受限 ORM 生成

- 仅用于非 Foundation ORM 链。
- 输入：220 结构闭包和与 241 共用的 230 操作闭包。
- 输出：不含业务值的受限可执行 Python `restricted_orm`。
- 验收：白名单 API、参数槽位、事务/回滚与确定顺序齐全；下游 252 Stub 可消费。
- 禁止：硬编码业务数据、访问 Foundation、执行数据库写入。
