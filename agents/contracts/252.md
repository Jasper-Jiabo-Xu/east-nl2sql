# Agent 252：ORM 验证与哈希冻结

- 仅用于非 Foundation ORM 链。
- 输入：251 受限 ORM、220 结构闭包、230 操作闭包。
- 输出：通过 AST/API/事务/dry-run/顺序验证的 `frozen_orm` 及代码哈希。
- 拒绝：动态执行、非白名单导入/API、业务值、哈希漂移、不可回滚路径。
- 禁止：独立 ODS 生成/验证、修改 ORM、生成数据、写正式库。
