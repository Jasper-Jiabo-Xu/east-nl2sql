# Agent 140 合同：查询规格构造

## 生产者

140-查询规格agent

## 消费者

150-Codex question-sql主生成agent、170-DeepSeek审核agent、180-GLM审核agent、220-结构闭包构造agent、260-回归agent

## 输入包

| 包 | 生产者 | Schema |
|---|---|---|
| PENALTY-FACT-PACKAGE | 120 | `contracts/packages/penalty-fact-package.schema.json` |
| EAST-OBSERVABLE-FACT-PACKAGE | 130 | `contracts/packages/east-observable-fact-package.schema.json` |
| DEEPSEEK-REVIEW-RESULT | 170（任务 2 回退） | `contracts/packages/deepseek-review-result.schema.json` |
| GLM-REVIEW-RESULT | 180（任务 2 回退） | `contracts/packages/glm-review-result.schema.json` |

## 输出包

| 包 | Schema |
|---|---|
| QUERY-SPECIFICATION-PACKAGE | `contracts/packages/query-specification-package.schema.json` |

## 包字段清单

| 字段 | 含义 | 约束 |
|---|---|---|
| `query_spec_id` | 查询规格标识 | `^qspec-[0-9]{3}$`；由 `run_id+qa_id` 确定，跨版本稳定 |
| `penalty_fact_package_ref` | 处罚事实包引用 | 不可变三元组 |
| `observable_fact_package_ref` | 可观察事实包引用 | 不可变三元组 |
| `query_goal` | 查询目标 | 非空 |
| `must_preserve_fact_refs` | 必须保留事实引用 | penalty_fact_id 列表，覆盖所有 yes/conditional |
| `main_object_and_grain` | 主对象和粒度 | 非空 |
| `query_entry` | 查询入口 | 入口表+条件 |
| `related_objects_and_path` | 关联对象与穿透路径 | 有序 |
| `filters_and_evidence` | 筛选条件及证据 | 每项含证据引用 |
| `return_fields` | 返回字段 | 非空数组 |
| `aggregation_dedup_sort_time` | 聚合去重排序时间 | GROUP BY 必填 |
| `observability_boundary` | 可观察性边界 | 可回答+不可回答 |
| `expected_result_shape` | 预期结果形态 | 行粒度+列集+聚合形态 |
| `sql_schema_scope` | SQL 表字段范围 | 每张表及每个字段均在可观察事实的映射矩阵或相关表字段中 |
| `minimum_positive_count` | 最少正例数量 | ≥1 |
| `minimum_negative_count` | 最少反例数量 | ≥1 |
| `condition_coverage` | 条件覆盖要求 | 每谓词正反例类型 |
| `code_value_coverage` | 码值覆盖要求 | 字段+目标码值列表 |
| `expected_row_group_count` | 结果行数/分组数 | min≤target，min≤low≤target≤high |
| `join_expansion_limit` | JOIN 发散上限 | multiplier>0, rows≥1 |
| `query_specification_package_schema_version` | 包协议版本 | `query-specification-v1` |

## 版本与血缘

- `version`：同一 `query_spec_id` 下从 1 递增；任何修改生成新版本，禁止覆盖；`query_spec_id` 不得由 attempt/version 派生。
- `supersedes_ref`：首次版本为空；迭代版本保存前一版本的不可变三元组。
- `attempt_no`：1、2、3；超过 3 进入人工审核。
- 任务 1 的 `parent_artifact_refs` 顺序固定为 `[penalty, observable]`；输入的 `run_id`、`qa_id`、`trace_id` 必须完全一致。
- 审核回退的 `parent_artifact_refs` 顺序固定为 `[penalty, observable, review, previous_spec]`；`input_hashes` 必须逐项对应该顺序。
- 第 3 次 attempt 的有效重构仍为 `candidate`；仅第 3 次重构未通过确定性校验时才生成 `blocked_manual`，超过 3 次拒绝。

## 审核回退路由

- 仅接受 170/180 审核包中 `error_types` 包含 `QUERY_SPEC_ERROR` 且 `route_suggestion=140` 的回退。
- 审核包的 `reviewed_package_ref` 必须精确等于被审核查询规格包三元组。
- 每次回退生成新版本，保留原始输入包引用不变。
