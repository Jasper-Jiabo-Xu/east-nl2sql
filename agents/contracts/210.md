# Agent 210：问题—SQL 会审与发布候选装配

- 输入：冻结问题/SQL、上游 manifest；回环时接收 260 回归包。
- 输出：送 220 的会审包，或送 010 的 `release_candidate`。
- Foundation：任务画像必须声明数据库版本、CA-V0.3.0、TRG-V1.0.0、目标对象类与数量。
- 拒绝：输入哈希/版本漂移、未通过 260、未知字段、敏感数据拟进入 Git。
- 禁止：生成业务数据、ORM、自由 SQL，或绕过 010 写正式库。
