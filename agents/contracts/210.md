# Agent 210：问题—SQL 会审与发布候选装配

- 输入：冻结问题/SQL、上游 manifest；回环时接收 260 回归包。
- 输出：送 220 的会审包，或送 010 的 `release_candidate`。
- Foundation：唯一运行期意图为 `foundation_task_package/v1`（producer=210，consumer=220/241/260）。它固定任务 ID、initial_seed/expansion、触发原因、目标数据库/对象/表字段范围/数量、分布、层次资产、禁止类型、恢复 QA 引用、CA-V0.3.0、TRG-V1.0.0。
- `foundation_profile/v1` 仅是从完整任务包可复算的兼容投影；必须携带相同 `foundation_task_ref`，不得作为 260 输入或替代完整任务包。
- 拒绝：输入哈希/版本漂移、未通过 260、未知字段、敏感数据拟进入 Git。
- 禁止：生成业务数据、ORM、自由 SQL，或绕过 010 写正式库。
