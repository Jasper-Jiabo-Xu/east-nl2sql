# Agent 241：绑定数据生成

- 事件输入：220 结构闭包、230 操作闭包、只读数据库快照引用。
- Foundation 输入：210 任务画像、220 结构闭包、CA-V0.3.0、TRG-V1.0.0、只读快照；不得接收操作闭包。
- 输出：稳定槽位绑定的 `bound_data`，交 242 验证。
- 验收：同输入可复算；单字段、表内、跨表与对象—明细—状态顺序约束成立。
- 禁止：调用旧生成器/策略器/装配器、生成 ORM、写正式库。
