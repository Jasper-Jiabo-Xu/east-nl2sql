# Agent 000：资产检索

- 输入：版本化查询、CA-V0.3.0/TRG-V1.0.0 locator 与 manifest 哈希。
- 输出：最小必要约束/引用证据、版本和内容哈希；下游 220 Stub 必须可消费。
- 拒绝：未知版本、哈希漂移、越权目录、敏感或可复原业务记录、无法唯一命中的查询。
- 禁止：生成或修改数据/ORM、返回旧 registry 或独立 ODS 作为事实源、写正式库。
