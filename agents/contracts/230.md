# Agent 230：事件操作闭包

- 仅用于非 Foundation 事件链。
- 输入：220 结构闭包与冻结问题/SQL。
- 输出：唯一 `operation_closure`，消费者必须同时包含 241 与 251。
- 验收：操作、依赖、顺序、事务边界和拒绝原因机器可解析，下游双 Stub 均通过。
- 禁止：Foundation 调用、生成数据、生成 ORM、产生两套消费者特化语义。
