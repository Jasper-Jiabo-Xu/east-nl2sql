# Agent 260：数据库 copy 绑定与 SQL 回归

- 输入：事件数据链的 242 包，或 ORM 链的 252 包；Foundation 只接收 242 包。
- Foundation：仅调用 `east-foundation-insert-compiler/v1`，固定生成参数化 INSERT 批次。
- 输出：数据库 copy 回归报告、SQL 结果、版本与批次哈希，回送 210。
- 验收：事务原子性、失败回滚、幂等/冲突、Gold SQL 和下游候选 Stub 均通过。
- 禁止：修改上游数据/ORM、自由生成 SQL、直接写正式库。
