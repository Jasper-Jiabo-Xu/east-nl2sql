# Agent 220：结构闭包

- 输入：210 会审包、版本化约束资产与带类型引用图。
- 输出：唯一 `structure_closure`，供 230、241、251、252、260 消费。
- Foundation：只展开 INITIAL_SEED/ON_DEMAND_ONLY 合法结构，EVENT_OWNED 写入必须拒绝。
- 验收：表、列、类型、主外键、码值与引用边均可追溯到 CA-V0.3.0/TRG-V1.0.0。
- 禁止：生成记录、ORM、INSERT 或修改数据库。
Foundation 只消费 210 `foundation_task_package/v1`；产出的 `structure_closure` 必须保留同一 `foundation_task_ref` 三元组。`foundation_profile/v1` 仅可作为经复算的兼容投影，不能独立启动闭包。Foundation 的最小字段集必须逐项等于任务 `target_table_field_scope` 的规范化 `TABLE.FIELD` 集合；000 的真实 CA-V0.3.0/TRG-V1.0.0 结果只能扩展该集合，绝不能删除任务字段或按 DDL 扩张整表。
