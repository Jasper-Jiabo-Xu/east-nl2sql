# EAS-37 010 验收报告（模板）

## 结论

本地实现与脱敏联调通过，等待工程助理提交工作树后触发 010 的真实最小任务。
不得记录真实数据库、原始材料或可复原键。

## 必须证据

- 输入：`release_candidate`、事件 `database_copy_regression` 或 Foundation 回归报告的内容哈希；
- 输出：`FORMAL-RELEASE-RECEIPT` 内容哈希、发布前后版本和分表聚合写入数；
- 测试：event/Foundation 成功、幂等、版本冲突、哈希漂移、写入失败回滚、唯一入口及 SQL 回退；
- 下游：210 候选消费和 110 SQL 回退消费的 Stub 结果；
- 风险：正式 SQLite 只在受控运行数据面调用固定代码，禁止把数据库或键写入本报告。

## 本轮证据

- 基线：`03dfe3bdaa9ff414520c35a96e7c839fccd33f56`。
- 新回执 Schema SHA-256：`f6cef0b4cf757689a615dec8f44805ddd62a2cf6e31f6e217a9675f227a89db4`。
- 固定提交器 SHA-256：`ecf1518f74a28e96570fb28fa8d553da53c58457b38c57bcfe40beecf6b223a0`。
- 本地命令：`PYTHONPATH=src:. python3 -m unittest tests.agents.010.test_committer tests.contracts.test_stage10_package_contracts tests.agents.210.test_scheduler tests.agents.260.test_regression -v`，46 项通过。
- 全库回归：`PYTHONPATH=src:. python3 -m unittest discover -s tests`，354 项通过。
- 覆盖：event 与 Foundation 成功提交、同候选幂等、版本冲突、冻结哈希漂移、第二个固定写入失败的整体回滚、SQL 执行错误唯一转 110、单入口和状态汇总。
- Multica Agent：`1d9153fc-4386-42e9-b2e5-56eb38f671af`，Codex runtime、`gpt-5.6-terra`、high、并发 1、无 Skill/MCP/自定义环境。

## 待工程动作

实施负责人无 Git 提交和 Agent 调度授权。工程助理提交本工作树后，必须以脱敏 fixture
对该 UUID 触发一次最小任务，要求执行 `tests.agents.010.test_committer` 并在结果中记录
Agent UUID、head SHA、命令和通过结果；不得在 Git 或 Issue 中附真实 SQLite/键。

## 可移植性返工（SOL-REVIEW）

- 移除了 `tests.agents.260.test_regression` 的运行时导入。
- 新增 `src/east_v5/agents/010/sanitized_foundation_fixture.py`：只调用生产模块
  210→220→241→242→260，在内存 SQLite 上生成并返回真实、已冻结的 Foundation
  回归报告；不导入测试包、不访问真实数据库。
- Fixture helper SHA-256：`11934f82d3d2f8c9105b5c8c15621d9fbec2e2e030cccf5e9f1cd46a904e8fcc`。
- 010 测试 SHA-256：`64e0af37d1de866e7c929b49f5a802f17eb0c05864c2f423095e3c5c4ffd80f2`。
- 已通过：`python3 scripts/v5.py check`；`PYTHONPATH=src python3 -m unittest tests/agents/010/test_committer.py -v`（5 项）；`PYTHONPATH=src python3 -m unittest discover -s tests`（354 项）。
