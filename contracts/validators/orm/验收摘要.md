## EAS-18 / 252 验收摘要

`freeze_orm` 只接收 251 `restricted_orm` 与关联 230 `operation_closure`。它在执行前校验通用信封、251/230 身份、CA-V0.3.0、TRG-V1.0.0、治理 manifest 内容哈希和关联引用；Foundation、未知字段、哈希漂移、跨 attempt、`blocked_manual` 均显式拒绝。

验证器只解析 AST，不执行输入代码、不触碰数据库或文件系统。允许的 API 被固定为事务、三种参数化 ORM 操作和末尾回滚；所有值必须为稳定 `params["slot_…"]` 占位符。输出为确定性 252 `frozen_orm`，携带代码/规则哈希、验证证据和空失败列表；下游 260 Stub 合同由测试实际消费。

脱敏正例在 `tests/validators/orm/fixtures/valid-event-orm.json`；仅含虚构表名、占位符和冻结的结构性哈希。
