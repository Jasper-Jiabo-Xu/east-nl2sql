<!--
  EAST NL2SQL V5 标准 PR 模板。
  所有字段为必填；发布到 05_新版本交付层/ 的 PR 必须通过全部门禁。
-->

## 变更清单

<!-- 逐项列出本次 PR 包含的变更，包括新增、修改、删除的文件及其目的 -->

- [ ]
- [ ]

## 来源引用与上游包

<!-- 引用本 PR 所依赖的所有 source_ref 和上游 artifact_id + version + content_hash -->

## 验收结果

<!-- 逐项确认，每个 check box 必须打勾 -->

- [ ] 输入来源、上游包、代码版本和模型/Prompt 版本可追溯
- [ ] 固定 JSON/Schema、字段存在性、来源引用和内容哈希验证通过
- [ ] 目标阶段规定审核全部通过（预审 / 双审核 / 硬编码验证 / 回归）
- [ ] 不存在未解决的冲突、待审项、失效引用或无效字段
- [ ] 输出文件位于声明目录内，未写入 `01_来源冻结层/` 或其他禁止目录
- [ ] 未修改 `05_新版本交付层/` 既有资产及其历史版本（本 PR 若新增交付物，走发布门禁）

## 安全扫描结果

<!-- 粘贴或引用安全扫描报告的关键发现；无发现也需声明"通过" -->

- [ ] 安全扫描通过
- [ ] 未包含 API Key、.env、Token、个人 SSH 凭据、真实 CoreBank 数据、LLM 原始响应缓存
- 扫描摘要（来源文件 / 工具 / 时间）：

## Manifest 哈希

<!-- 若本 PR 包含交付物，提供 content_hash 和完整 manifest 引用 -->

- `delivery_content_hash`：
- `package_id`：
- Manifest 文件路径：

## 审批依据

<!-- 引用批准本 PR 的治理合同条款和审批人 -->

- 治理合同引用（`00_治理与合同/` 中的具体条款）：
- 审批人：
- 审批日期：

## 是否发布到 05_新版本交付层/

- [ ] 否 —— 本 PR 仅修改过程层、公共代码、治理或测试
- [ ] 是 —— 已满足全部发布门禁（manifest 哈希 + 安全扫描 + 验收报告 + 人工批准 + 本 PR 合并）

## 发布门禁确认（仅在发布到 05 时填写）

- [ ] 发布清单已生成，包含全部交付文件哈希
- [ ] 安全扫描报告无阻断性发现
- [ ] 对应验收报告已签发
- [ ] 人工审批已完成（审批人签名/记录链接）
- [ ] 本 PR 将通过受保护分支合并，不直接推送 main
