# 05 新版本交付层 发布总账

## 发布规则（项目负责人 2026-08-12 裁定，见 `00_治理与合同/架构快照/V5最终共识与Issue迁移合同.md`）

- `05_新版本交付层/` 仅保留各能力的**最新版**；历史版本降级归档至 `03_构建过程层/`。
- 对确定性通过的整包**无需项目负责人或 Codex 另行审批**，可直接提交/推送/合并。
- 仍必须满足：来源引用、manifest、哈希、安全扫描、SQLite/JSON 校验、验收；异常项必须先人工裁决为 0。
- 新版本进入 05 前，旧版本降级到 03，05 仅保留最新版。

## 已发布资产

| 资产类型 | 版本目录 | artifact_id | 状态 |
| --- | --- | --- | --- |
| 约束资产 | `约束资产/CA-V0.1.0-foundation` | `CA-FOUNDATION-20260805-001` | 已发布 |
| 约束资产 | `约束资产/CA-V0.2.0-foundation` | `CA-FOUNDATION-20260805-002` | 已发布 |
| 约束资产 | `约束资产/CA-V0.3.0-multifield` | `CA-MULTIFIELD-20260812-003` | 已发布 |
| 带类型引用图 | `带类型引用图/TRG-V1.0.0` | `EAS-TYPED-GRAPH-20260812-001` | 已发布（冻结） |

## 消费约定

- 约束资产为**能力分包**（V0.1=数据元/码表/机构部门树，V0.2=+会计科目树，V0.3=多字段约束），不得仅凭版本号推断覆盖关系，按各 manifest 的 `capabilities` 与 `not_in_scope` 取用。
- 带类型引用图（TRG-V1.0.0）为 Foundation 边界判断、最小铺底计算、241 引用取值与 260 写入排序的统一事实输入；不生成业务值、不写正式数据库。图冻结后由 EAS-42 消费裁定 Foundation 资格，铺底时或业务（监管处罚）生成时再据实修订。
- 各资产详情见各自 `manifest/asset_manifest.json` 与版本内说明。
